Install
=============
script/plugin install git://github.com/allenwei/Fixtures-Import.git

Tasks
============
rake db_utils:export                 # Export data from tables in the current environment db to fixtures (YML format).
rake db_utils:export_shift           # Export data from tables in the current environment db to test/fixtures/export (YML format).
rake db_utils:import                 # Populates the current environment's database with fixtures in db/fixtures folder.
rake db_utils:reset_db               # Reset database and populate with intial data.
